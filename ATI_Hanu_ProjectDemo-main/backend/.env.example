## Copy this file to `.env` and edit values for your environment
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=staff_platform

# Backend server port
PORT=4000

# JWT secret used by the backend (change for production)
JWT_SECRET=dev_secret_change_me
PORT=4000
JWT_SECRET=dev_secret_change_me
